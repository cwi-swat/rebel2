module sync::double::Follower

spec Follower 
  times: Integer;
  
  init event start() 
    post: 
      this.times' = 0;
   
  event hit(times: Integer)
    post: this.times' = this.times + times;
  
  states : 
    (*) -> active : start;
    active -> active : hit;