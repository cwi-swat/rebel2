module sync::double::Follower

spec Follower 
  times: Integer;
  
  event start() 
    post: 
      this.times' = 0;
   
  internal event hit(times: Integer)
    post: this.times' = this.times + times;
  
  states : 
    (*) -> active : start;
    active -> active : hit;