module sync::multi::Follower

spec Follower 
  nrOfHits: Integer;
  
  init event start()
    post: this.nrOfHits' = 0;
  
  event hit() 
    post: this.nrOfHits' = this.nrOfHits + 1;
    
  states:
    (*) -> active: start;
    active -> active: hit;
   