module sync::Follower

spec Follower 
  times: Integer;
  
  init event start() 
    post: this.times' = 0;
    
  event hit()
    post: this.times' = this.times + 1;
 
  states:
    (*) -> active: start; 
    active -> active: hit;     