module hotel::SmallHotel

spec Key

spec Room
  key: Key;
  
  init event create(initialKey: Key) 
    post: this.key' = initialKey; 
    
  event checkKey()
  
  states:
    (*) -> created: create;
    created -> created: checkKey;