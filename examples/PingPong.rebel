module PingPong

spec PingPong 
  times: Integer;
  
  init event start() 
    post: 
      this.times' = 0;
  
  event hit()
    post: 
      this.times' = this.times + 1; 
    
  states:
    (*) -> ping: start;
    ping -> pong: hit;
    pong -> ping: hit;